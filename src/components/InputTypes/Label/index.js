import styled from "styled-components";

const Label = styled.label`
  display: flex;
  align-items: center;
  font-weight: bolder;
  padding: 0;

  input,
  textarea {
    margin-right: 0.5rem;
  }
`;

export default Label;
